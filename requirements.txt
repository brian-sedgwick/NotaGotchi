# Not-A-Gotchi Dependencies
# Python 3.7+ required

# GPIO and Hardware
RPi.GPIO>=0.7.1
gpiozero>=1.6.2
spidev>=3.5
pi-ina219>=1.4.0

# Note: lgpio is optional but recommended for newer Pi systems
# Install via: sudo apt install python3-lgpio
# gpiozero will automatically use it if available, otherwise falls back to RPi.GPIO

# Image Processing and Display
Pillow>=9.4.0

# Bluetooth Low Energy (for social features)
bleak>=0.21.0

# Note: sqlite3 is included in Python standard library (no separate install needed)
